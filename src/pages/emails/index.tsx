import React from 'react';
import { Typography } from '@material-ui/core';
import AppLayout from '../../components/app-layout';

const DashboardPage = () => {
  return (
    <AppLayout>
      <Typography variant='h3'>The Emails Page</Typography>
    </AppLayout>
  );
};

export default DashboardPage;
